export const CODEQL_DISCOVERY_HINT = `AutoVul research tools are available in this Pi session. For structured vulnerability-research hypotheses, use autovul_research (validate or execute) and autovul_run (status, cancel, or replay). Choose capability=flow when the decisive fact is source-to-sink value propagation. Choose capability=missing_check when the decisive fact is that a protected operation is reachable without a required check or CFG dominance; do not encode a missing check as fake taint flow. Choose capability=typestate when the decisive fact is one resource's finite, ordered lifecycle and identity-backed transition evidence; do not encode protocol order as fake taint flow or a missing check. The current MissingCheck boundary is deliberately narrow: direct calls and direct checks, dominance within one named function or callback CFG, and a single source file. The current Typestate boundary is deliberately narrow: one local resource, finite states, ordered events, one prohibited transition, direct lexical identity evidence, and bounded single-function scope. The host generates and revises hypotheses; AutoVul returns deterministic observations, a decision, and revision hints, but does not generate the next hypothesis, start an Agent Loop, or write a Finding. codeql_database, codeql_workflow and codeql_query remain compatibility tools for existing CodeQL workflow and Query Pack work; they are not a third primary research interface. Do not require the user to type /codeql-generate. Keep work inside the host Pi Agent Loop.`;

const GENERATE_GUIDANCE = `Use AutoVul M4 inside the host Pi Agent Loop.

Do not ask the user to write JSON. Read the supplied project source, vulnerability description/patch, vulnerable database, and optional fixed database with the host's file tools, then convert them into a validated VulnerabilitySpec and use the native CodeQL tools:
1. Inspect the supplied vulnerable/fixed databases with codeql_database and select the matching Language Pack: Python, JavaScript/TypeScript, Java/Kotlin, or C/C++. The workflow language field has only the canonical values python, javascript, java, cpp; use javascript for TypeScript and java for Kotlin. Include the required top-level cwe field in the VulnerabilitySpec (for example CWE-78 for command injection); put strict_semantics inside validation, never at the VulnerabilitySpec root. Set project_root to the supplied source root. Never create a database or read a reference query.
2. Before workflow start, read the relevant source and vulnerability/patch, determine one concrete Source and Sink, and populate validation.source and validation.sink file/line before workflow start for every user-provided case. The validation objects are semantic expectations, not full matchers: they may contain only label, description, kind, file, line, and symbol. Put matcher-specific fields such as module, type, member, name, property, argument_index, argument_name, keyword_name, and keyword_value only in the candidate intent.source/intent.sink (and in additional_flow_steps), never in validation.source/validation.sink. Do not invent top-level source_file/source_line/sink_file/sink_line fields; exact locations belong in validation.source.file/line and validation.sink.file/line. Record the matcher kind and any call-site condition that makes the sink security-relevant. Use nl -ba immediately before starting and re-open the selected lines: for a multiline function definition, the source parameter line is the line containing the parameter identifier; for a multiline call, a whole-call endpoint is the callee invocation line. For Python and JavaScript/TypeScript call_argument-like endpoints, use the selected argument expression line. For C/C++, use call_argument to select the argument data-flow node but freeze the enclosing FunctionCall's invocation line/file; the C/C++ renderer deliberately anchors location constraints to that call site, and the probe result is authoritative. Do not subtract one line for the def/callee line, and do not use a JSDoc comment, function comment, type-only line, import, wrapper definition, try statement, or enclosing assignment. Workflow start freezes these expectations, so do not start with approximate lines. Cross-check that the chosen source line actually contains the source identifier or binding and that the chosen sink line actually contains the callee/argument endpoint; if it does not, keep reading the numbered source and correct the line before starting. For a local promisified wrapper, distinguish the imported API name (for example exec) from the local invocation name (for example execAsync) and use the actual invocation endpoint. For Python keyword-sensitive sinks, use keyword_name and keyword_value in the structured matcher (for example subprocess.run(..., shell=True) is a call sink with module: "subprocess", name: "run", argument_index: 0, keyword_name: "shell", keyword_value: true). Do not invent a location; if the location is not known yet, keep reading the supplied source instead of starting a weak case. A fixed database only adds differential verification; it does not make Source/Sink location validation optional.
3. Tool input schemas are strict. Every structured object uses the exact schema_version v2.contracts/1 (a dot between v2 and contracts, not v2/contracts/1). For codeql_query, the top level may contain only action, run_id, and exactly one of intent or candidate; never add top-level rationale, description, or other explanation fields. Put rationale inside intent or candidate only where its schema allows it. Start exactly one workflow for this case with codeql_workflow action=start. Do not change spec_id or start another workflow to bypass the case budget. Then call codeql_query action=probe with the concrete Source/Sink intent. A failed/empty probe is feedback for repairing the matcher; it is not permission to invent a query. Do not use a broad “any call” matcher to force a hit. For Python and JavaScript/TypeScript call_argument matchers, the location is the selected argument expression's endpoint line. For C/C++, the selected argument remains the data-flow endpoint, but the location constraint is the enclosing call's invocation line; use the line reported by the probe. If a location-constrained call_argument probe is empty, retry with the concrete endpoint reported by the relevant language pack or temporarily omit only the location constraint to discover it, then copy the returned endpoint into the intent.
4. After a passed probe, submit a structured TaintQueryCandidate containing the same intent and the exact returned probe_evidence (candidate_id, query_id, spec_id, language, intent, probe_evidence, round, origin); never submit raw ql_text to Pi tools. Call codeql_query action=draft first. This LSP draft check does not consume the formal candidate budget. Use its line/column diagnostics and definition/hover/completion feedback to repair syntax/import/type/API issues.
5. Only a clean LSP draft should normally enter codeql_query action=verify. The CLI compile/analyze result is final authority. Use probe evidence and the returned repair_brief as the only repair direction; if the result says QUERY_SEMANTIC_MISMATCH, compare the observed flow endpoints with the probed Source/Sink before changing syntax. Submit at most 3 formal candidates for the case, one revision per round, and stop when the case summary says budget_exhausted. An LSP timeout/degraded result may fall back to CLI and must be reported as degraded.
6. Finalize with codeql_workflow action=finalize only after the vulnerable/fixed policy passes. A missing fixed database is a warning; report the result as reproduced, not differential.

Python-specific feedback: use an imported module/member matcher only when the sink is actually an imported API; for a repository-local wrapper such as safer_popen, use a local call matcher with name/member plus exact file, enclosing symbol, and invocation line. Python parameter matchers should include exact file, symbol, and line when the source is one specific formal, especially for **kwargs. When a Python flow crosses a dynamic wrapper, variadic *args/**kwargs boundary, or an explicit conversion/container call, use additional_flow_steps with a structured call_argument matcher to select the exact argument node (argument_index or argument_name) and connect it to the callee parameter or next call; do not write raw QL and do not broaden the matcher to any call. Enumerate every confirmed boundary in order: argument into a callee is call_argument -> parameter, a conversion/container input to its return is call_argument -> call, a helper return into the next invocation is call -> call_argument, and a dynamic wrapper's argument into the variadic callee is call_argument -> parameter. Do not jump from an outer source directly to the final sink when the source inspection shows intermediate calls. For receiver methods such as join on a separator expression, use a repository-local call matcher with no module plus exact file, enclosing symbol, and call/argument line; do not label an object method as a builtins or imported module API unless the probe confirms that API identity.

Language Packs intentionally differ: Python uses new DataFlow/ApiGraphs, JavaScript/TypeScript uses JavaScript DataFlow API, Java/Kotlin uses callable/type matchers, and C/C++ distinguishes expressions, pointers, indirection, and struct/class field reads. Matcher support is strict: Python supports call, call_argument, function, parameter, and environment; JavaScript/TypeScript supports call, function, constructor, property, environment, and parameter but does not support call_argument; Java/Kotlin supports call, function, constructor, property, environment, and parameter but does not support call_argument; C/C++ supports call, call_argument, function, array_index, array_element, parameter, property, and environment. Use only kinds supported by the workflow Language Pack for the endpoint and every additional_flow_steps edge. In C/C++, array_index means the untrusted index expression returned by ArrayExpr.getArrayOffset(), while array_element means the value produced by the subscript expression itself (the ArrayExpr); use array_element for content such as buffer[i] or pat[i], and do not use array_index merely because brackets appear in the source. For JavaScript/TypeScript, a destructured callback value such as async ({ port }) => ... is a PropRead, so use a property matcher for port, not a parameter matcher; a normal named formal parameter is a parameter matcher. For a typed destructured parameter, distinguish the property-binding line ('{' / 'container_id'), the type-annotation line, and the enclosing function line: the Source validation line and source_location must equal the CodeQL probe endpoint for the property binding, not the later type annotation line. Keep the candidate intent Source/Sink aligned with the Source/Sink frozen in validation after workflow start. If a candidate intent includes source.file/source.line or sink.file/sink.line, those locations must match the frozen validation locations; the host rejects endpoint drift before CLI verification. A probe may expose an internal forwarding endpoint or an intermediate transformation; do not replace the frozen semantic endpoint with that intermediate node. Preserve the frozen Source/Sink matchers and model intermediate probe endpoints as directed additional_flow_steps. Only copy a probe location into a candidate Source/Sink when it is the same semantic endpoint already frozen by validation; otherwise use it to refine a boundary edge. A matcher symbol means the enclosing function or method name, not the callee/API name; omit symbol for an anonymous callback or arrow function. For C/C++, a formal parameter may be represented by the DataFlow parameter node itself, while a propagated use may be a VariableAccess; use the concrete parameter name and enclosing function, and add file/line only after confirming the probe endpoint. Use call_argument for an exact callee argument when the argument location is the security-relevant endpoint; use call for a whole invocation. Use a property matcher for a concrete field such as a network or protocol field; standard memcpy/strcpy-family names are normalized to common compiler fortified builtins during rendering. For Java/Kotlin constructors, put the fully qualified class name in type (for example java.lang.ProcessBuilder); do not split it into module and type. When a Java flow crosses an object constructor and field, use additional_flow_steps with directed {from,to} edges, not the legacy additional_flow list. A setter-to-object-field path should make the edges explicit, for example: parameter argInFileEnding -> property Executrix.inFileEnding, then constructor TempFileNames argument 2 -> property TempFileNames.inputFilename; if the fixed implementation sanitizes the setter, add its cleanFileEnding as a sanitizer. Before workflow start, derive Source/Sink file and line from numbered source (nl -ba). Never use a JSDoc/comment, import, type-only annotation, try/branch/assignment, wrapper definition, or nearby argument as a validation endpoint; use the line containing the actual parameter/property binding or callee invocation. For a local wrapper around an imported API, use the local wrapper's invocation endpoint, not the imported API name or wrapper definition. Validation lines must be the actual CodeQL endpoint start lines, and the workflow must not start until those lines are known. Never read a reference query, never create a database, never run a target-project build, and never claim success without a passing verification. Report the deterministic case_summary and repair_brief rather than counting turns from memory.
`;

const GENERIC_C_CPP_FLOW_GUIDANCE = `For C/C++, a formal parameter may be represented by the DataFlow parameter node itself or by a VariableAccess at its declaration; use the concrete parameter name, enclosing function, and confirmed file/line together. Use call_argument for an exact callee argument when the argument location is the security-relevant endpoint; use call for a whole invocation. For a conversion source -> f(x), use an additional_flow_steps edge from call_argument x to call f (the call matcher denotes the conversion return value). For a wrapper argument entering a variadic *args/**kwargs-like boundary, use call_argument -> parameter, and for a pointer/array boundary use the concrete probed call argument or formal parameter at each function boundary; do not replace a real boundary with an unbounded any-call matcher. If source/sink probes pass but a direct CLI candidate has zero flow, inspect the exact probed nodes and add only the confirmed missing boundary as additional_flow_steps (for example a concrete property -> call_argument edge); this is a real C/C++ data-flow modeling boundary, not permission to broaden the matcher. When source inspection shows multiple confirmed boundaries, enumerate them in order and use one directed structured edge per boundary; do not jump from an outer source directly to the final sink. For receiver method calls, use a local matcher with no module plus exact file, enclosing symbol, and endpoint line unless the language pack probe confirms a library API identity.`;
const STRICT_ENDPOINT_GUIDANCE = "For a new strict user case, set validation.strict_semantics=true and include validation.source and validation.sink with only label, description, kind, file, line, and optional symbol. Put the full structured matcher (module/type/member/name/property/argument_index/argument_name/keyword constraints) in intent.source and intent.sink, not in validation. Do not invent source_file/source_line/sink_file/sink_line fields. Use the exact matcher kinds that will be probed (for example parameter and call_argument). A whole-call line and an argument endpoint line are different semantics; do not freeze one kind and submit another. If the security-relevant value is an argument, freeze the call_argument kind and selected argument index; for C/C++, freeze the enclosing call's invocation line because the argument node is selected semantically while the renderer anchors its location to the FunctionCall.";
const GENERIC_DYNAMIC_PYTHON_GUIDANCE = "For Python dynamic wrappers, do not stop at the outer call. A complex wrapper may need more than eight explicit edges; use up to sixteen confirmed additional_flow_steps rather than skipping a real boundary. For a variadic formal, use the line containing the parameter identifier such as *args or **kwargs, not the def line; when overload declarations precede an implementation, use the implementation signature line containing the actual variadic parameter. Then inspect each helper call that consumes or returns the value: parameter -> helper call_argument at the actual helper invocation line, helper call_argument -> helper call for the returned value, helper call -> the next concrete call_argument, and that call_argument -> the callee parameter at the line containing that formal identifier. For a nested expression such as a join result passed directly into split, prefer the shortest directed edge actually confirmed by the probe (for example call_argument of join -> call_argument of split); do not invent a call -> call edge merely because a helper expression is nested. A preparation statement such as list.extend or an enclosing assignment is not the execute call endpoint; use the line containing the actual callee invocation and selected argument. Continue this chain through command/list unpacking until the frozen sink; every candidate with additional_flow_steps must include the final boundary into the frozen sink, not only a valid-looking prefix.";
const PYTHON_ARGUMENT_INDEX_GUIDANCE = "For every positional call_argument matcher, count argument_index from the actual left-to-right call expression after reading the numbered source; never assume the tainted value is argument 0. If a wrapper call has several preceding positional values, use the real index, while keyword arguments use argument_name. Verify that the selected argument line contains that expression. Apply this to every wrapper boundary, not only the final sink.";

export function generationPrompt(workspace: string, request: string): string {
  return GENERATE_GUIDANCE + "\n\n" + GENERIC_C_CPP_FLOW_GUIDANCE + "\n\n" + STRICT_ENDPOINT_GUIDANCE + "\n\n" + GENERIC_DYNAMIC_PYTHON_GUIDANCE + "\n\n" + PYTHON_ARGUMENT_INDEX_GUIDANCE + "\nWorkspace: " + workspace + "\n\nUser case:\n" + request;
}
